{
  "name": "ads-kiosk-brin",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace @ads-kiosk/shared && npm run build --workspace @ads-kiosk/admin && npm run build --workspace @ads-kiosk/kiosk && npm run build --workspace @ads-kiosk/server",
    "start": "node apps/server/dist/index.js",
    "bootstrap:admin": "npm run bootstrap:admin --workspace @ads-kiosk/server",
    "backup": "npm run backup --workspace @ads-kiosk/server",
    "restore": "npm run restore --workspace @ads-kiosk/server --",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test --workspaces --if-present -- --run",
    "test:e2e": "playwright test",
    "typecheck": "npm run build --workspace @ads-kiosk/shared && npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
